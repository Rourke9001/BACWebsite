# api/

Azure Functions for the static site. Planned: contact/service form handler (BAC-8/9) —
honeypot + rate limiting, email via Microsoft 365 to info@baclogistics.co.za.
Secrets (mail credentials) go in SWA application settings / `local.settings.json`, never in git.
